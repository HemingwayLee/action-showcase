name: helloworld

on:
  push: {branches: main}
  pull_request: {branches: main}

jobs:
  test:
    runs-on: ubuntu-latest
    name: Echo
    steps:
      - run: echo "hello, action"
      - name: show ls and pwd
        run: |
          ls
          pwd
